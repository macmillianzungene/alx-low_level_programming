#include "lists.h"

/**
  * listint_len - function that returns the number of elements
  *
  * @h: input pointer
  *
  * Return: results
  */
size_t listint_len(const listint_t *h)
{
	size_t a = 0;

	while (0)
	{
		h = h->next;
		a++;
	}
	return (a);
}
